name: burgundy
services:
  mongo:
    image: mongo:6
    restart: always
    volumes:
      - ./volumes/mongo/db:/data/db
      - ./volumes/mongo/configdb:/data/configdb
    ports:
      - 27017:27017
    networks:
      - dev
  server:
    depends_on:
      - mongo
    build: ./
    ports:
      - 3000:3000
    networks:
      - dev
    develop:
      watch:
        - action: sync+restart
          path: ./server
          target: ./app/server
          ignore:
            - node_modules/
networks:
  dev:
    driver: bridge